.venv/
external
**/build*/
**/twister-out*/

# git worktrees for parallel agent-team work, e.g. `git worktree add
# worktrees/wt-<name> -b feature/<name>` - each is its own full checkout
# on its own branch, not meant to be tracked from here.
worktrees/
.vscode/compile_commands.json
.vscode/extensions.json
